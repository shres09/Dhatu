<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dhatu">


    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dhatu">

        <activity android:name=".OpenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".firstSequence.QuestionPage"/>
        <activity android:name=".firstSequence.boyC"/>
        <activity android:name=".firstSequence.girlW"/>
        <activity android:name=".firstSequence.fruitW"/>
        <activity android:name=".MainActivity"/>

        <activity android:name=".firstSequence.WebView1"/>
        <activity android:name=".firstSequence.WebView2"/>
    </application>

</manifest>